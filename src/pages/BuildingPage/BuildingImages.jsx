import React from 'react';

const BuildingImages = ({ params }) => {
  // const imagesLinks = params?.formattedValue?.map((item) => item.url);
  return (
    <div>
      <h3>Clcik Image</h3>
    </div>
  );
};

export default BuildingImages;
